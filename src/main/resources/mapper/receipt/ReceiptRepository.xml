<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mybatis.generalTest.receipt.repository.ReceiptRepository">

    <select id="findOne" parameterType="ReceiptId" resultType="ReceiptEntityDto">
        SELECT
            ID,
            REQUEST_ID,
            TITLE,
            CONTENTS,
            RECEIPT_DATE,
            STATUS,
            RECEPTIONIST_ID
        FROM MYBATIS_RECEIPT
        WHERE ID = #{id}
    </select>

    <insert id="create" parameterType="ReceiptEntityDto" useGeneratedKeys="true">
        INSERT INTO MYBATIS_RECEIPT (
            REQUEST_ID,
            TITLE,
            CONTENTS,
            RECEIPT_DATE,
            STATUS,
            RECEPTIONIST_ID
        ) VALUES (
                #{requestId},
                #{title},
                #{contents},
                #{receiptDate},
                #{status},
                #{receptionistId}
                          )
    </insert>

    <update id="update" parameterType="ReceiptEntityDto">
        UPDATE MYBATIS_RECEIPT
        SET
            REQUEST_ID = #{requestId},
            TITLE = #{title},
            CONTENTS = #{contents},
            RECEIPT_DATE = #{receiptDate},
            STATUS = #{status},
            RECEPTIONIST_ID = #{receptionistId}
        WHERE ID = #{id}
    </update>
</mapper>